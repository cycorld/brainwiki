<%= form_for(@starting) do |f| %>
  <% if @starting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@starting.errors.count, "error") %> prohibited this starting from being saved:</h2>

      <ul>
      <% @starting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="input-field col s12">
      <select name="start" class="browser-default">
        <% unless @prev.nil? %>
          <option value="<%=@prev.id%>"><%=@prev.title%></option>
          <option disabled>──────────</option>
        <% end %>
        <% @nuerons.each do |n| %>
          <option value="<%=n.id%>"><%=n.title%></option>
        <% end %>
      </select>
    </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :subtitle %><br>
    <%= f.text_field :subtitle %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
